import React from 'react';
import TaskList from './TaskList';

const App = () => {
  return <TaskList />;
};

export default App;
// The App component is now a simple wrapper for the TaskList component.